package com.tomasthrawat.gamearchitecture

class Collision {
    fun resolve(car: CarState) {
        // Replace with the game's actual broad-phase/narrow-phase collision system.
        // Collision must remain independent from rendering and UI.
    }
}
