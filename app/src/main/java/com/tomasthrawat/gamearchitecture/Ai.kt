package com.tomasthrawat.gamearchitecture

class Ai {
    fun update(dt: Float) {
        // AI should calculate intent/input from world state.
        // It should not manipulate Compose UI or renderer nodes directly.
    }
}
